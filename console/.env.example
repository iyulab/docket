# docket-core instance the console's dev proxy forwards /api/* to.
# Defaults to a local instance if unset. Do not point this at a shared
# docket-core (e.g. a production host) unless you mean to.
VITE_DOCKET_CORE_URL=http://127.0.0.1:8420
